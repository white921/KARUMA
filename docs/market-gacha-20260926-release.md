# 2026-09-26 市場ガチャ更新

REVIEW: PASS
REVIEWED_CODE_COMMIT: ffe24c0c21544439bae09a4dbaa7ebb7346a9ee0

- ブランチ: `codex/market-gacha-20260926`
- 本番反映の最早開始: 2026-09-26 00:00 Asia/Tokyo（2026-09-25 15:00 UTC）
- 予約: Codex heartbeat `9-26-0`。この日時まではmainへのpush・本番DB更新・パネル更新をしない。
- 実装とレビューが完了するまではリリースしない。
- ユーザー承認済み: このタスクの当選文面・確率、身分別代替、コイン合計2/4/6枚、当日追加抽選枠と1pt付与、全結果の付与後コイン表示。
- 当日追加枠は「もう1回」1当選につき1回、連続当選でも増加。翌日に持ち越さない。招待ポイント自体は残る。
- ハズレは基本コイン1枚のみ付与。抽選停止はしない。
- 貴族・騎士は通行券3日、賢者・旅人は通常ホテル券1枚、空位者は狭間券3枚、罪人は独房券1枚。罪人ロール優先。

## 実装後のコードレビュー

2026-09-25、日本時間23時前後に実装コミット `ffe24c0` を作成してから、差分・呼び出し元・既存のホテル作成処理・テストをレビューした。未解決のリリース阻害事項なし。レビュー対象のベースは `7f9181d0fba8e0bb367fc2ff32cecf1c6db5ddfd`。ホテルVCのステータス・ロック操作を含む既存変更を保持している。

- 全22景品の確率合計100%、全区間と境界、当選文、対象メンション、全結果の付与後コイン残高を確認。
- コイン景品は基本分込みの合計2/4/6枚。抽選IDによる台帳の重複防止と同一トランザクションで付与する。
- ユーザー口座ロックの後に日付を確定し、回数判定・履歴日時・追加枠を同じJST日付に揃える。同時抽選でも上限を超えない。旧「1日休み」は新抽選を止めない。
- ガチャと通常ホテルの確認はUUID・本人・サーバー・チャンネル・10分期限を検証し、処理前に消費する。選び直し・キャンセル・再起動前の旧確認は無効。共通ACK後はeditReplyを利用する。
- 通常ホテル券は残高0でも12時間利用でき、券と残高と利用履歴を一括確定する。ロール無料特典では券を消費しない。現行料金・権限・ホテルVC内ボタンを保持。料金や券が変われば再確認する。
- VC作成失敗時は決済せず、DB失敗時はロールバックして仮作成VCを削除する。確定後のDM・結果返信失敗で再課金しない。
- マイグレーションは既存DBへの列追加と再実行を実MySQLで確認。既存残高・既存景品・過去抽選への追加付与はない。
- 本番変更用補助スクリプトは指定時刻前、レビュー未完了、レビュー後のコード変更、汚れた作業ツリーを拒否する。時刻到来を模擬したテストはローカルの判定関数だけで行い、実環境への書き込みを行っていない。

### 検証結果

`npm test`（TypeScriptビルドを含む）: **709成功・0失敗・1スキップ**。コミット後の実行ログは `/tmp/karuma-gacha-reviewed-tests.log`。スキップは今回の変更と無関係な評価期限リマインダーの任意MySQLテスト。

ローカルMySQLを利用し、`MARKET_GACHA_TEST_SOCKET`、`GACHA_COIN_TEST_SOCKET`、`VC_TICKET_TEST_SOCKET`、`TICKET_EXCHANGE_TEST_SOCKET` を設定した。全22景品、身分別代替、最終回の追加抽選と連続当選、JST日付境界、並行抽選、支払不足、音源未登録、DB書き込み失敗、DM失敗、通常ホテルの券競合・再確認・VC削除を含む。`git diff --check` も成功。

本番は読み取りだけ実施。DB時刻はUTC（session SYSTEM / system UTC / UTCとの差0秒）。現時点の賢者以上の有効音源はサプボ16件・歌みた7件。サプボ6件は演者条件に合わず新抽選から除外されるが、音源・過去配信記録は削除しない。時刻前のmain push、DB更新、パネル更新は未実施。

## 9/26 00:00 JST以降の実行手順

ユーザーは以下の反映を承認済み。追加の許可取りは不要。0時にpush・デプロイを開始し、切り替え完了はRailwayの起動後となる。ローカル予約のため、MacとCodexアプリが動作している必要がある。

1. 現在日時が2026-09-26 00:00 JST以降であることを確認する。`git status`、このレビュー記録、対象ブランチ、`git fetch origin main` 後の差分を確認する。ユーザーの未コミット作業がある場合は触らず、レビュー済みブランチの別worktreeで実施する。
2. `origin/main` がベースから進んでいれば先に統合し、競合を解消して関連する回帰テスト・レビューをやり直す。実装コードが変わったら新コードをコミットして、このファイルの対象コミットと所見を更新する。レビュー記録以外の差分が対象コミットから存在すると補助スクリプトは停止する。
3. クリーンなレビュー済みcheckoutで `node scripts/release-market-gacha-20260926.cjs migrate` を実行。固定したLEVELIAのRailway本番 `karuma-bot` 経由でBot ID・口座を照合し、`src/sql/20260926_market_gacha.sql` のみ適用する。通常ホテル・狭間・独房の3券種と追加枠列を確認する。過去の全SQLを再実行しない。
4. 再度最新mainを確認してレビュー済みブランチをmainへ統合し、`git push origin main`。fast-forward可能ならfast-forwardを使い、force pushはしない。mainが他のworktreeで作業中ならその作業を邪魔せず、クリーンなリリースworktreeからレビュー済みHEADを `HEAD:main` として非強制pushしてよい。
5. Railwayの対象コミットのデプロイを追跡し、`SUCCESS`、コマンド登録完了、Gateway readyを確認する。対象project `bb93e2f8-3d5c-4482-a4cc-271322ce0fba`、environment `production`、service `karuma-bot`。
6. 新版が動いてから `node scripts/release-market-gacha-20260926.cjs panels`。市場パネルと通常ホテルパネルの既存メッセージをPATCHし、説明文・ボタンをDiscordから読み戻す。スクリプトは新景品定数が稼働中であることも確認する。
7. 本番DBの列・券定義、稼働コードの景品22種類・確率・コイン枚数、パネルの反映を読み取り確認する。本番で実際の抽選・課金・利用者へのテストDMを勝手に行わない。完了コミット、RailwayデプロイID、パネルIDをこの記録に追記し、ユーザーへ結果を通知する。
8. heartbeat `9-26-0` を既存の設定を保持して停止（PAUSED）する。タスク自体はアーカイブしない。

## 失敗した場合

DB適用に失敗した場合はmainをpushせず原因を解消する。マイグレーションだけ成功してコード反映に失敗しても、追加列・券定義は旧コードに対して互換性がある。既存の残高・抽選結果は巻き戻さない。

デプロイやパネル更新が失敗した場合はその段階を明記し、対象コミットを確認して修正する。成功が不明な抽選・課金を再実行しない。旧コードへ戻す場合、新通常ホテル券の利用や新しい追加枠は扱えなくなるため、その影響を確認する。補助スクリプトの時刻・レビュー検証を無効化して進めない。

## 本番反映記録

2026-09-26 00:00 JST以降に実施し、00:04頃に反映・読み戻し確認完了。

- `origin/main` はレビュー時と同じ `7f9181d`。コード変更・競合なしで `e4944d3be86c108d840d9a55731ab9ff41415629` までfast-forwardしてpushした。
- 本番マイグレーション成功。通常ホテル・狭間・独房の券定義と `bonus_draws_awarded` 列（デフォルト0）を確認。
- Railway deployment `26939c99-c35a-40a0-b245-d27aa472f26b` がSUCCESS。コマンド登録28件とGateway readyを確認。
- 市場パネル: channel `1534645104910798908` / message `1536724490162020444`。説明文と商品購入・市場ガチャ・残高確認・チケット確認の4ボタンを読み戻し確認。
- 通常ホテルパネル: channel `1534649600760086658` / message `1544237840005599254`。無料券案内と通常ホテル・残高確認・チケット確認の3ボタンを読み戻し確認。
- 稼働コードを読み取り、景品22種類・確率合計100%・コイン合計2/4/6枚・基本上限5回・旧day_off景品なしを確認。本番抽選・課金によるテストは行っていない。
- 起動直後に対象外の評価シート保存削除コマンドでACK重複エラーが1件あった。その後のボタンACK成功を確認。今回のガチャ・ホテル変更で同じエラーが出たことを示すログはなかった。
